---
"dsar": minor
---

Add `dsar webhooks dlq list`, `dsar webhooks dlq replay`, and `dsar webhooks dlq replay-all` for exhausted outbound webhook attempts. Bulk replay accepts `status=dead` in addition to `failed`.
