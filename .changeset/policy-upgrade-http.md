---
"dsar": patch
---

Declare the `POST /policies/upgrades/propose` JSON body (`fromVersion`, `tenantId`, `toVersion`, optional `workspaceId`) on OpenAPI and the Node SDK, and document the live policy upgrade approval workflow.
