const Page = () => (
	<main>
		<h1>DSAR deletion webhook</h1>
		<p>
			POST signed <code>request_fulfilled</code> events to{" "}
			<code>/api/webhooks/dsar</code>.
		</p>
	</main>
);

export default Page;
